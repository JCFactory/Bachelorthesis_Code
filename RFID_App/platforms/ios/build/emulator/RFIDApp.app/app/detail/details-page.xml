<Page class="page" loaded="loaded" 
    xmlns="http://schemas.nativescript.org/tns.xsd">
    <ActionBar class="action-bar">
        <Label class="action-bar-title" background-color="green" text="Detail-Information"></Label>
    </ActionBar>
     <!-- <ListView items="{{drugs}}">
            <ListView.itemTemplate>
                <Button text="{{id}}" tap="onTap" class="btn"/>
                 <Button text="{{countryCode}}" tap="onTap" class="btn"/>
                  <Button text="{{id}}" tap="onTap" class="btn"/>
                   <Button text="{{id}}" tap="onTap" class="btn"/>
            </ListView.itemTemplate>
        </ListView> -->
    <GridLayout rows="50, 50, 50, 50, 50, 50" columns="*, *">
        <Label text="ID" col="0" row="0" class="detail"/>
        <Label text="123456" col="1" row="0" class="detail"/>
        <Label text="Name" col="0" row="1" class="detail"/>
        <Label text="{{name}}" col="1" row="1" class="detail"/>
        <Label text="Countrycode" col="0" row="2" class="detail"/>
        <Label text="{{countryCode}}" col="1" row="2" class="detail"/>
        <Label text="Size" col="0" row="3" class="detail"/>
        <Label text="{{size}}" col="1" row="3" class="detail"/>
        <Label text="Location" col="0" row="4" class="detail"/>
        <Label text="{{location}}" col="1" row="4" class="detail"/>
        <Label text="TimeStamp" col="0" row="5" class="detail"/>
        <Label text="{{timeStamp}}" col="1" row="5" class="detail"/>
    </GridLayout>
</Page>
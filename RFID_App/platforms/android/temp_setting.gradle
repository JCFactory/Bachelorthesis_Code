android {
            defaultConfig {
                applicationId = "org.nativescript.RFIDApp"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }